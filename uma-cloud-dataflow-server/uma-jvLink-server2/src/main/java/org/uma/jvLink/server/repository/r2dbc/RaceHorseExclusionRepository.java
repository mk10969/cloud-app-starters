package org.uma.cloud.job.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.uma.cloud.common.model.RaceHorseExclusion;

public interface RaceHorseExclusionRepository extends JpaRepository<RaceHorseExclusion, String> {
}
